DROP TRIGGER IF EXISTS Sconto_Scaglione;

DELIMITER //
CREATE TRIGGER Sconto_Scaglione
AFTER INSERT ON Sconto
FOR EACH ROW
BEGIN
	IF NOT EXISTS (SELECT Sconto FROM Scaglione 
	WHERE Scaglione.Sconto = NEW.Id)
	THEN INSERT INTO Scaglione (Sconto) VALUES (NEW.Id);
	END IF;
END;//
DELIMITER ;

